# Correção: Visibilidade das Abas para Perfis de Manifestação (Chefia, Coordenação, etc.)

## 1. Contexto
O usuário relatou que ao acessar o painel de um processo logado como "Chefia" (perfil simulado ou real), a navegação renderizava apenas a "Aba 3" em vez da "Aba 7" (Visão Painel), que é onde de fato ocorrem as manifestações, aprovações e assinaturas do fluxo de trabalho gerencial.

## 2. Diagnóstico
No `ProcessoController@getAbasDoPerfil`, as permissões de renderização do menu de navegação (Nav) estavam configuradas para exibir apenas a aba `[3]` para os perfis gerenciais: `Chefia`, `Coordenação`, `Superintendência`, `Equipe C.G.`, `Coordenação-Geral`, `Direção` e `CDE`. 

O controlador corretamente os redirecionava para `aba=7` ao abrir o processo (devido ao `getAbaEStatus`), mas o arquivo `show.blade.php` mascarava a Aba 7 na barra superior porque a variável `$abasDoPerfil` não continha o número 7 no array.

## 3. Solução Implementada
Atualizamos a matriz de mapeamento em `getAbasDoPerfil` para que todas as instâncias de manifestação e deliberação recebessem o array `[1, 2, 3, 7]`.
Desta forma:
1. Eles terão acesso ao ícone da **Aba 7 (Visão Painel)**, que é a interface oficial de assinatura e devolução.
2. Eles terão acesso nativo de visualização às **Abas 1, 2 e 3** no Nav, permitindo que leiam os dados preenchidos pelas equipes de Destinação e Caracterização antes de proferirem suas decisões.

## 4. Histórico de Alterações e Rollback

### Alteração 1: `app/Http/Controllers/ProcessoController.php`

**Estado Anterior (Antes):**
```php
    private function getAbasDoPerfil(string $perfil): array
    {
        $map = [
            'Equipe Destinação'   => [1, 3],
            'Equipe Caracterização' => [2],
            'Chefia'              => [3],
            'Coordenação'         => [3],
            'Superintendência'    => [3],
            'Equipe C.G.'         => [3],
            'Coordenação-Geral'   => [3],
            'Direção'             => [3],
            'CDE'                 => [3],
        ];

        return $map[$perfil] ?? [];
    }
```

**Estado Novo (Depois):**
```php
    private function getAbasDoPerfil(string $perfil): array
    {
        $map = [
            'Equipe Destinação'   => [1, 3],
            'Equipe Caracterização' => [2],
            'Chefia'              => [1, 2, 3, 7],
            'Coordenação'         => [1, 2, 3, 7],
            'Superintendência'    => [1, 2, 3, 7],
            'Equipe C.G.'         => [1, 2, 3, 7],
            'Coordenação-Geral'   => [1, 2, 3, 7],
            'Direção'             => [1, 2, 3, 7],
            'CDE'                 => [1, 2, 3, 7],
        ];

        return $map[$perfil] ?? [];
    }
```

**Plano de Rollback / Desfazer:**
1. Desfaça a modificação no `ProcessoController.php`, retornando os arrays dos perfis de manifestação de `[1, 2, 3, 7]` para `[3]`.
